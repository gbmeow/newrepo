{
  "name": "docker-centos-simple-server",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "description": "Node.js Hello World app on CentOS using docker",
  "author": "Noname",
  "dependencies": {
    "express": "3.2.4"
  }
}